"""Data layer: contracts, acquisition helpers, and preprocessing logic.

This subpackage owns the canonical field contracts for jaguar metadata
(``contracts``), the feline pipeline contract constants
(``pipeline_contract``), raw-data acquisition and consensus FASTA generation
(``acquisition``), and the preprocessing/tokenization pipeline
(``preprocessor``) that transforms genomic sequences into DNABERT-2-ready
tokenized windows.
"""
